import './Hero.css';

function Hero() {
  return (
    <section className="hero">
      <div className="hero-inner">
        <h2 className="hero-title">Now Playing</h2>
        <p className="hero-subtitle">The latest movies, ready to browse.</p>
      </div>
    </section>
  );
}

export default Hero;
